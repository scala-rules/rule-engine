package org.scalarules.finance

/**
  * Aggregates the implicit definitions of the different parts of the DSL. This makes it possible for users
  * of the DSL to include just one `import`-statement before using it.
  *
  * {{{
  *   import org.scalarules.dsl.en.finance._
  * }}}
  */
package object en extends FinanceDsl {

}
